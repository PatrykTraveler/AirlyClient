package Airly;

public enum Error {

    UNKNOWN_ERROR,
    INPUT_VALIDATION_ERROR,
    UNAUTHORIZED,
    FORBIDDEN,
    NOT_FOUND,
    UNEXPECTED_ERROR,

    PARSE_ERROR,
    DATA_ERROR,
    SECURITY_ERROR,
}
